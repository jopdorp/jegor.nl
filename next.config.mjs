/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',  // Ensure static export mode
  trailingSlash: true,  // Optional: Add trailing slashes for all routes
};
  
export default nextConfig;
